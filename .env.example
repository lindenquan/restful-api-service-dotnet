# Environment Variables
# Copy this file to .env and update values as needed
#
# This file is loaded by:
#   1. The API application (via DotNetEnv) - for all configuration
#   2. Docker Compose - for service configuration

# ===========================================
# Application Configuration
# ===========================================

# ASP.NET Core environment (loaded by the API at startup)
# Valid values: local, dev, stage, prod, eu-prod, amr-prod, eu-stage, amr-stage
ASPNETCORE_ENVIRONMENT=local

# ===========================================
# MongoDB Configuration
# ===========================================
# These override appsettings.json values using ASP.NET's environment variable convention
# Format: SectionName__PropertyName (double underscore)

MongoDB__Username=admin
MongoDB__Password=password123

# ===========================================
# Docker Compose Configuration
# ===========================================
# These are used by docker-compose.yml for container setup

MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=password123
MONGO_DATABASE=prescription_db

# Redis Configuration
REDIS_PORT=6379

# ===========================================
# Root Admin Configuration
# ===========================================
# Initial API key for root admin user (created on first startup)
# IMPORTANT: Change this to a secure value!
RootAdmin__InitialApiKey=root-api-key-change-in-production-12345

